#!/bin/sh
# Pre-commit lint temporarily disabled while lint-staged-projects.js is being hardened.
# Full gate is at pre-push: format:check + lint + typecheck.
# TODO: restore when integration tests pass.
exit 0
